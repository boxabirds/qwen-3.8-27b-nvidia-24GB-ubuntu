# Meteor Rush — no-thinking variant

Same prompt and same model as [`../meteor-rush`](../meteor-rush), but generated
with the server started as `THINKING=0` (reasoning disabled, instruct sampling
preset).

See [`../meteor-rush/README.md`](../meteor-rush/README.md) for the full
comparison.
